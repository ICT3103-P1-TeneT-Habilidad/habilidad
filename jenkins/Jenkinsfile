pipeline {
    options {
        disableConcurrentBuilds()
    }
    // agent {
    //     docker {
    //         image 'node:lts-buster-slim'
    //         args '-p 3000:3000'
    //     }
    // }
    environment {
        CI = 'true'
    }
    stages {
        stage('Build') {
            steps {
                sh 'npm install'
                sh 'chmod +x -R ./jenkins/scripts'
            }
        }
<<<<<<< HEAD
        
        stage('OWASP Dependency Check'){
            steps{
                dependencyCheck additionalArguments: '''-s ./habilidad-client -o ./ --format HTML --format XML --suppression suppression.xml''', odcInstallation: 'DependencyCheck'
=======
        // stage('Checkout SCM'){
        //     steps{
        //         git '/home/JenkinsDependencyCheckTest'
        //     }
        // }
        stage('OWASP Dependency Check'){
            steps{
                sh 'pwd'
                sh 'ls -la /var/jenkins_home/tools/org.jenkinsci.plugins.DependencyCheck.tools.DependencyCheckInstallation/DependencyCheck/bin'
                dependencyCheck additionalArguments: '--format HTML --log /var/jenkins_home/log.log', odcInstallation: 'DependencyCheck', stopBuild: true
>>>>>>> 26706af311654f4c2ac2a6e3579c886eaaf36412
            }
        }
        stage('Test') {
            steps {
                sh './jenkins/scripts/test.sh'
            }
        }
        // stage('Deliver') {
        //     steps {
        //         sh './jenkins/scripts/deliver.sh'
        //         input message: 'Finished using the web site? (Click "Proceed" to continue)'
        //         sh './jenkins/scripts/kill.sh'
        //     }
        // }
    }
    post {
		success {
			dependencyCheckPublisher pattern: 'dependency-check-report.xml'
		}
	}
}
